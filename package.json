{
  "name": "pvgis-mcp",
  "version": "1.0.0",
  "private": true,
  "description": "Remote MCP server (Cloudflare Worker) per PVGIS — produzione fotovoltaica via API Commissione Europea",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "start": "wrangler dev"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "agents": "^0.0.80",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "wrangler": "^3.90.0",
    "typescript": "^5.5.0",
    "@cloudflare/workers-types": "^4.20240000.0"
  }
}
